# Simplified Implementation Plan

## Текущее состояние

### ✅ Уже есть
1. **S3 Storage**: 17 книг в облаке (Beget Cloud)
2. **MCP Markdown**: Конвертация PDF/DOCX → Markdown (встроенный инструмент)
3. **GigaAgent**: LangGraph + GigaChat архитектура готова
4. **ArangoDB**: В cleaning_finance_app есть пример работы с графовой БД
5. **Glossary**: 25 терминов
6. **Templates**: Шаблоны методологий

## 🎯 Упрощенный подход

### Шаг 1: Конвертация книг в Markdown (используем MCP)
```bash
# Скачать книгу из S3
s3cmd get "s3://bucket/books/book.pdf" cache/books/

# Конвертировать через MCP Markdown
# (это делается автоматически через tools)
```

### Шаг 2: Создать агента в giga_agent
Добавить нового агента `methodology_agent` по аналогии с `landing_agent`:
- Читает markdown из книги
- Использует GigaChat для извлечения структуры
- Генерирует outline.yaml
- Заполняет шаблоны

### Шаг 3: Сохранить в ArangoDB
Использовать паттерн из cleaning_finance_app для сохранения графа методологий

## 📋 План действий

### Week 1: Подготовка данных
1. Скачать 3-5 книг из S3 в cache/books/
2. Конвертировать в Markdown через MCP
3. Положить в inputs/books_markdown/

### Week 2: Создать methodology_agent
1. В giga_agent/backend/graph/giga_agent/agents/ создать methodology_agent/
2. Реализовать граф: classify → extract → generate → validate
3. Использовать system prompt из agent.md
4. Интегрировать с glossary

### Week 3: Тестирование
1. Прогнать 3 книги через агента
2. Получить 3 методологии
3. Валидация через validate_glossary.py

## 🛠️ Инструменты

**Используем существующие**:
- MCP Markdown (встроен в VSCode) - для конвертации
- giga_agent - для AI обработки
- GigaChat - для LLM
- ArangoDB - для хранения (опционально на старте)

**НЕ создаем**:
- ❌ Свой OCR pipeline
- ❌ Свой extractor
- ❌ Дублирование LangGraph

## 🚀 Следующий шаг

Хочешь:
1. Сначала скачать и конвертировать 3-5 книг в markdown?
2. Или сразу создать methodology_agent в giga_agent?
3. Или другой подход?
